# Copy to .env.local and fill in from your Supabase project:
#   Supabase dashboard → Project Settings → Data API
# The anon key is a publishable client key, not a secret — data is protected
# by Row Level Security, which must be enabled on every table.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
